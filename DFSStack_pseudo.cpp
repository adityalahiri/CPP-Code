#include <bits/stdc++.h>
using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    for x in child[node]:
        vis[x] = 1;
        push_child of (x) in stack

    return 0;
}
